apiVersion: v1
kind: Service
metadata:
  name: third-eye-service
spec:
  selector:
    app: third-eye
  ports:
    - protocol: TCP
      port: 8008
      targetPort: 8008
      nodePort: 32008
  type: NodePort